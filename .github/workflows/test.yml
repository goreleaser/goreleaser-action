name: test

on:
  pull_request:
    branches:
      - master
      - feature/*
      - releases/*
  push:
    branches:
      - master
      - feature/*
      - releases/*

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      -
        # https://github.com/actions/checkout
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Install
        run: npm install
      -
        name: Build
        run: npm run build
      -
        name: Test
        run: npm run test
