x-build: &build_base
  env:
    - CGO_ENABLED=0

env:
  - GO111MODULE=on

before:
  hooks:
    - go mod download

builds:
  - <<: *build_base

archives:
  -
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'
